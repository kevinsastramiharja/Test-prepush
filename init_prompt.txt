For the rest of this conversation, strictly enforce the following operating rules:

### 1. Execution & Git Safeguards
- [SAFEGUARD-CODE] Never edit or create files unless the user explicitly commands "go code!". Exactly one "go code!" grants permission for exactly one coding session.
- [SAFEGUARD-GIT] Never push to any remote repository (`git push`).
- [GIT-TRANSPARENCY] Every git command executed locally must be explicitly stated in your response before or alongside the output.
- [HOOK-CHECK] Verify that a pre-push hook blocking direct pushes to master/main exists. If missing, inform the user and request execution of:
  `sudo /Users/kevin.sastramiharja/prepush/Test-prepush/install_prepush.sh`
- [NON-DESTRUCTIVE] Never execute destructive terminal commands (e.g., `rm -rf`, `git reset --hard`, `git clean -f`, dropping databases/tables) without explicit user confirmation.

### 2. Failure Philosophy: "Fail Loud & Exhaustive"
- [NO-SILENT-DEFAULTS] Avoid defaults that mask missing or invalid data when specific values are expected. Prefer direct access like `the_dict["key"]` over `the_dict.get("key", default)`. Let missing keys fail loudly immediately.
- [EXHAUSTIVE-BRANCHING] Any `if/elif` inspecting a domain state or category must terminate in an `else` branch logging an actionable error:
  `logger.error(f"[ERROR/EXCEPTION]: Unhandled branch or state: {val} | Context: {context}")`
  *(Note: Only include `traceback.format_exc()` if an active exception is currently in scope).*
- [ERROR-PREFIX] For unskippable errors or issues demanding engineer attention:
  - Always prefix log messages with: `"[ERROR/EXCEPTION]: "`
  - In `except` blocks, capture the full stack trace (using `logger.exception(...)` or `traceback.format_exc()`).

### 3. Investigation & Verification Workflow
- [PLAN-FIRST] For any non-trivial logic or multi-file changes, diagnose and present the implementation plan/architecture first. Wait for confirmation before asking to proceed.
- [SELF-CHECK] After executing code changes, run targeted validation (e.g., run the relevant `pytest`, `python -m unittest`, or syntax/type checks on modified files) and report the exit code and output.
